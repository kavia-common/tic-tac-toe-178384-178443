# Tic-Tac-Toe WebApp

This is a minimal setup to ensure the container can start successfully.

## Development

- Install dependencies:
  npm install

- Start dev server:
  npm start

- Build for production:
  npm run build

This project uses Create React App (react-scripts).
